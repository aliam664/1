<UserControl x:Class="ACModHub.UI.Views.BackupsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <Grid><TextBlock Text="{DynamicResource BackupsTitle}" Style="{StaticResource PageTitle}"/><Button Content="{DynamicResource Refresh}" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" VerticalAlignment="Top"/></Grid>
  <DataGrid Grid.Row="1" ItemsSource="{Binding Backups}" SelectedItem="{Binding Selected}"><DataGrid.Columns><DataGridTextColumn Header="Created" Binding="{Binding CreatedAt, StringFormat=g}" Width="180"/><DataGridTextColumn Header="Mod ID" Binding="{Binding ModId}" Width="260"/><DataGridTextColumn Header="Files" Binding="{Binding RelativeFiles.Count}" Width="90"/><DataGridTextColumn Header="Storage" Binding="{Binding RootPath}" Width="*"/></DataGrid.Columns></DataGrid>
  <Grid Grid.Row="2" Margin="0,14,0,0"><TextBlock Text="{Binding Message}" Foreground="{StaticResource WarningBrush}"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="Restore" Command="{Binding RestoreCommand}" Margin="0,0,8,0"/><Button Content="Delete" Command="{Binding DeleteCommand}" Background="{StaticResource PrimaryBrush}"/></StackPanel></Grid>
 </Grid>
</UserControl>
