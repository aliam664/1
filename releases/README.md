# Releases

This folder is intentionally empty. Installer binaries (`AC-Mod-Hub-Setup.exe`) are build
artifacts produced by the Windows CI pipeline (`.github/workflows/release.yml`) and attached
to GitHub Releases only — no binaries or placeholder files are committed to the repository.

Published releases: <https://github.com/aliam664/1/releases>
